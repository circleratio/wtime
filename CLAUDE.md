# wtime

CLIの時刻表示ツール（世界時計）。Python実装。

## ドキュメントの依存関係

このプロジェクトのドキュメントは一方向の依存関係を持つ。上流が確定してから下流を作成・更新する。ある文書を変更したときは、その下流にある文書・実装との整合性を確認すること。

```
doc/requirement.md   … 要求仕様（唯一の要求のソース。ここが変わらない限り仕様は変わらない）
        │  入力
        ▼
doc/spec.md           … 設計（要求仕様を満たすための実装方針・モジュール構成）
        │  入力
        ▼
doc/plan.md            … 実装計画（設計をステップに分解した作業手順）
        │  入力
        ▼
src/, tests/            … 実装（計画のステップを実行した結果）
        │  参照
        ▼
README.md              … 利用者向けドキュメント（要求仕様の使用例＋実装で判明した実行環境の知見を反映）
```

| ファイル | 役割 | 上流（入力にする） | 下流（変更時に確認する） |
|---|---|---|---|
| `doc/requirement.md` | 要求仕様 | なし | `doc/spec.md`, `README.md` |
| `doc/spec.md` | 設計 | `doc/requirement.md` | `doc/plan.md` |
| `doc/plan.md` | 実装計画 | `doc/spec.md` | `src/`, `tests/` |
| `src/`, `tests/` | 実装 | `doc/plan.md` | `README.md` |
| `README.md` | 利用者向けドキュメント | `doc/requirement.md`, 実装 | なし |

補足:
- 実装段階で計画からの逸脱（実行環境の制約など）が発生した場合、実装だけでなく `README.md`（場合によっては `doc/spec.md`/`doc/plan.md`）側の記述も実態に合わせて修正する。逆方向（下流の都合で上流の要求仕様を書き換える）は行わない。
- 要求仕様（`doc/requirement.md`）を変更した場合は、設計・計画・実装・READMEの中に古い前提のまま残っている箇所がないか順に辿って確認する。

## 開発フロー

要求仕様定義 → 設計 → 実装計画作成 → 実装 → ドキュメント作成、という一連の流れの進め方は `.claude/skills/spec-driven-dev/SKILL.md` に手順化してある。機能追加・仕様変更を行うときはこのスキルを起点にする。
